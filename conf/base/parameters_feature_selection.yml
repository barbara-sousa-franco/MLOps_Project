feature_selection:
  random_state: 42
  shap:
    top_n: 15
    threshold: null      # set a float to use a cutoff instead of top_n
  challenger:
    n_trials: 30
    # holdout on the validation set (X_val from split_train), consistent with model_selection