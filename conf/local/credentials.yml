# Here you can define credentials for different data sets and environment.
#
#
# Example:
#
# dev_s3:
#     aws_access_key_id: token
#     aws_secret_access_key: key
#
# prod_s3:
#     aws_access_key_id: token
#     aws_secret_access_key: key
#
# dev_sql:
#     username: admin
#     password: admin

# --- Hopsworks Feature Store (NEVER commit real values; file is gitignored) ---
# TODO: fill in locally. .env.example documents the keys without values.
hopsworks:
  api_key: ${oc.env:HW_API_KEY,null}
  project: ${oc.env:HW_PROJECT_NAME,null}
