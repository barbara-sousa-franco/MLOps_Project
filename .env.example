# Variáveis de ambiente — copiar para .env (gitignored) e preencher.
# O conf/local/credentials.yml lê estas vars via ${oc.env:...}.

# --- Hopsworks Feature Store ---
HW_API_KEY=
HW_API_NAME=
HW_PROJECT_NAME=MLOps_Project_BCCM

# --- MLflow (opcional; por defeito usa ./mlruns local) ---
# MLFLOW_TRACKING_URI=
